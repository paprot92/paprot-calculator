import { PreviousInputPipe } from './previous-input.pipe';

describe('PreviousInputPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviousInputPipe();
    expect(pipe).toBeTruthy();
  });
});
