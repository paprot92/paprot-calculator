import { CurrentInputPipe } from './current-input.pipe';

describe('CurrentInputPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentInputPipe();
    expect(pipe).toBeTruthy();
  });
});
